<header>
    <nav>

        <a
            class="brand"
            href="{{ _blog.url }}"
        >
            {% if _blog.logo_url %}
                <img src="{{ _blog.logo_url }}" alt="{{ 'logo' | lang }}" />
            {% endif %}
            <span class="name">{{ _blog.name }}</span>
        </a>


        <div class="nav-links">
            {% for nav in _blog.nav_header %}
                <a class="nav-{{ nav.name | lower | slug }}{% if is_current_url(nav.url) %} active{% endif %}" href="{{ nav.url }}">
                    <span>{{ nav.name }}</span>
                </a>
            {% endfor %}
        </div>
    </nav>
</header>